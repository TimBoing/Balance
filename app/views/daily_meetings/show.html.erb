<div class="container">
  <h1>Ok dude voilà le topo!</h1>
  <p>PIE : <%= "#{@physique}/#{@intellectuel}/#{@emotionnel}" %> </p>
  <p><%= @comments.gsub(/\n/, '<br />').html_safe %></p>
  <%= link_to "Écrire un peu plus?", edit_daily_meeting_path  %>
  <h1>Tes objectifs aujourd´hui:</h1>
  <%= link_to "Nouvel Objectif", new_daily_meeting_objective_path(@mamm) %>

  <% @objectives.each do |obj|%>
    <p><%= obj.description %></p>
  <% end %>

</div>
